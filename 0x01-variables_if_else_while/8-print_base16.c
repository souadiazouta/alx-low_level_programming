#include <stdlib.h>
#include <stdio.h>
/**
 * main - print all the numbers of 16 base  in lowercase .
 *
 * Return: Always 0.
 */
int main(void)
{
	char num;

	for (num = 0 ; num <= 'f' ; num++)
	{
		putchar(num);
	}
	putchar('\n');

	return (0);
}
